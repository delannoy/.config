
aiometer    # [A Python concurrency scheduling library, compatible with asyncio and trio](https://github.com/florimondmanca/aiometer)
            # aiometer                        A Python concurrency scheduling library, compatible with asyncio and trio
            # ├── anyio<5,>=3.2               High level compatibility layer for multiple asynchronous event loop implementations
            # │   ├── exceptiongroup>=1.0.2   Backport of PEP 654 (exception groups)
            # │   ├── idna>=2.8               Internationalized Domain Names in Applications (IDNA)
            # │   ├── sniffio>=1.1            Sniff out which async library your code is running under
            # │   └── typing-extensions>=4.1  Backported and Experimental Type Hints for Python 3.8+
            # └── exceptiongroup              Backport of PEP 654 (exception groups)

duckdb      # [DuckDB is an in-process SQL OLAP Database Management System](https://github.com/duckdb/duckdb)
            # duckdb  DuckDB embedded database

feather     # [Feather: fast, interoperable binary data frame storage for Python, R, and more powered by Apache Arrow](https://github.com/wesm/feather)
            # feather                                Feather AI SDK
            # ├── Flask==1.1.2                       A simple framework for building complex web applications.
            # │   ├── Jinja2>=2.10.1                 A very fast and expressive template engine.
            # │   │   └── MarkupSafe>=2.0            Safely add untrusted strings to HTML/XML markup.
            # │   ├── Werkzeug>=0.15                 The comprehensive WSGI web application library.
            # │   │   └── MarkupSafe>=2.1.1          Safely add untrusted strings to HTML/XML markup.
            # │   ├── click>=5.1                     Composable command line interface toolkit
            # │   └── itsdangerous>=0.24             Safely pass data to untrusted environments and back.
            # ├── Pillow==8.2.0                      Python Imaging Library (Fork)
            # ├── flask-cors==3.0.10                 A Flask extension adding a decorator for CORS support
            # │   ├── Flask>=0.9                     A simple framework for building complex web applications.
            # │   │   ├── Jinja2>=3.1.2              A very fast and expressive template engine.
            # │   │   │   └── MarkupSafe>=2.0        Safely add untrusted strings to HTML/XML markup.
            # │   │   ├── Werkzeug>=3.0.0            The comprehensive WSGI web application library.
            # │   │   │   └── MarkupSafe>=2.1.1      Safely add untrusted strings to HTML/XML markup.
            # │   │   ├── blinker>=1.6.2             Fast, simple object-to-object and broadcast signaling
            # │   │   ├── click>=8.1.3               Composable command line interface toolkit
            # │   │   ├── importlib-metadata>=3.6.0  Read metadata from Python packages
            # │   │   │   └── zipp>=0.5              Backport of pathlib-compatible object wrapper for zip files
            # │   │   └── itsdangerous>=2.1.2        Safely pass data to untrusted environments and back.
            # │   └── Six                            Python 2 and 3 compatibility utilities
            # ├── flask-restful==0.3.9               Simple framework for creating REST APIs
            # │   ├── Flask>=0.8                     A simple framework for building complex web applications.
            # │   │   ├── Jinja2>=3.1.2              A very fast and expressive template engine.
            # │   │   │   └── MarkupSafe>=2.0        Safely add untrusted strings to HTML/XML markup.
            # │   │   ├── Werkzeug>=3.0.0            The comprehensive WSGI web application library.
            # │   │   │   └── MarkupSafe>=2.1.1      Safely add untrusted strings to HTML/XML markup.
            # │   │   ├── blinker>=1.6.2             Fast, simple object-to-object and broadcast signaling
            # │   │   ├── click>=8.1.3               Composable command line interface toolkit
            # │   │   ├── importlib-metadata>=3.6.0  Read metadata from Python packages
            # │   │   │   └── zipp>=0.5              Backport of pathlib-compatible object wrapper for zip files
            # │   │   └── itsdangerous>=2.1.2        Safely pass data to untrusted environments and back.
            # │   ├── aniso8601>=0.82                A library for parsing ISO 8601 strings.
            # │   ├── pytz                           World timezone definitions, modern and historical
            # │   └── six>=1.3.0                     Python 2 and 3 compatibility utilities
            # ├── numpy==1.20.2                      NumPy is the fundamental package for array computing with Python.
            # ├── python-slugify==5.0.2              A Python Slugify application that handles Unicode
            # │   └── text-unidecode>=1.3            The most basic Text::Unidecode port
            # └── requests==2.25.1                   Python HTTP for Humans.
            #     ├── certifi>=2017.4.17             Python package for providing Mozilla's CA Bundle.
            #     ├── chardet<5,>=3.0.2              Universal encoding detector for Python 2 and 3
            #     ├── idna<3,>=2.5                   Internationalized Domain Names in Applications (IDNA)
            #     └── urllib3<1.27,>=1.21.1          HTTP library with thread-safe connection pooling, file post, and more.

pyarrow     # [Apache Arrow is a multi-language toolbox for accelerated data interchange and in-memory processing](https://github.com/apache/arrow)
            # pyarrow               Python library for Apache Arrow
            # └── numpy<2,>=1.16.6  Fundamental package for array computing in Python

tables      # [A Python package to manage extremely large amounts of data](https://github.com/PyTables/PyTables)
            # tables                 Hierarchical datasets for Python
            # ├── blosc2~=2.0.0      Python wrapper for the C-Blosc2 library.
            # │   └── msgpack        MessagePack serializer
            # ├── cython>=0.29.21    The Cython compiler for writing C extensions in the Python language.
            # ├── numexpr>=2.6.2     Fast numerical expression evaluator for NumPy
            # │   └── numpy>=1.13.3  Fundamental package for array computing in Python
            # ├── numpy>=1.19.0      Fundamental package for array computing in Python
            # ├── packaging          Core utilities for Python packages
            # └── py-cpuinfo         Get CPU info with pure Python
